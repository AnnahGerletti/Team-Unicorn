<form method="post" action="/">
  <div class="profiles">
    <div class="container">
      {{>heros}}
    </div>

    <div class="container">
      {{#each profiles}}
        {{>playerButton}}
      {{/each}}
    </div>
  </div>
</form>
